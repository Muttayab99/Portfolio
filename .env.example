# Server-side only. Never prefix with VITE_ (that would ship it to the browser).
# Local dev: copy to .env.local. Production: set in Vercel -> Project -> Settings -> Environment Variables.
GROQ_API_KEY=
